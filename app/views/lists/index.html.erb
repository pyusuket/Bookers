<h1>投稿一覧</h1>
  <% @lists.each do |list| %>

    <p>Title</p>
    <span>
      <%= link_to list.title, list_path(list.id) %>
    </span>
  
    <p>body</p>
    <span>
      <%= link_to list.body, list_path(list.id) %>
    </span>
  
  <% end %>
